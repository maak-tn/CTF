<%- include("partials/header") %>
    <section id="help-dialog">
        <div id="help-header">
            <h4>Hint</h4>
            <span id="help-exit-button">&times;</span>
        </div>
        <p>The router's login credentials were not modified. Use the router's type and name to try and learn the default login credentials</p>
    </section>
    <div id="overlay"></div>
        <main>
            <section id="level-count">
                <h3>Level 1</h3>
            </section>
            <section id="level-riddle" class="spacer">
                <p>The neighbor's kid keeps using my Wifi without permission 😠, it's time I changed the password. But it seems like I've forgoten the router's login.... maybe you could help me ? 🙏</p>
                <p>The router is a Huawei HG532e</p>
                <a href="/router-login-huawei">my router's login Screen</a>
            </section>
            <section id="level-submit" class="spacer">
                <form action="/9dolor3" method="POST">
                    <label>
                        <input type="text" id="key" placeholder="CTF[keyInHere]" name="key" required>
                        <button>
                            <i class="far fa-paper-plane"></i>
                        </button>
                    </label>
                </form>
            </section>
        </main>
<%- include("partials/footer") %>